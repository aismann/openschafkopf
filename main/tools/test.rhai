fn inspect(ahand, rules) {
    debug(ahand);
    debug(ahand[0]);
    debug(ahand[0]);
    debug(ahand[0].cards());
    //debug(ahand[0].cards()[0]);
    debug(rules);
    count(ahand[0], rules, 1);
    false
}
